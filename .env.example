# AxonGate 环境变量配置
# 复制此文件为 .env 并根据实际情况修改

# ============ 数据库配置 ============
# PostgreSQL 数据库密码（必需，请修改为强密码）
POSTGRES_PASSWORD=axongate_password

# 数据库名称
POSTGRES_DB=axongate

# 数据库用户名
POSTGRES_USER=axongate

# ============ Caddy 配置 ============
# ACME 证书邮箱（用于 Let's Encrypt）
ACME_EMAIL=admin@example.com

# 生产域名（用于 HTTPS 证书）
DOMAIN=ai.example.com

# ============ 端口配置（可选）============
# HTTP 端口（默认 80）
HTTP_PORT=80

# HTTPS 端口（默认 443）
HTTPS_PORT=443

# 调试端口（默认 8080，用于本地开发）
DEBUG_PORT=8080

# ============ 应用配置（可选）============
# 日志级别（debug | info | warn | error）
LOG_LEVEL=info

# ============ 安全提示 ============
# 1. 请务必修改 POSTGRES_PASSWORD 为强密码 并且同步修改 config/backend.yaml
# 2. 生产环境请配置真实的 ACME_EMAIL 和 DOMAIN
# 3. 不要将 .env 文件提交到 Git 仓库
# 4. 建议使用密钥管理服务（如 HashiCorp Vault）管理敏感信息
